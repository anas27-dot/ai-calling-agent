services:
  - type: web
    name: hindi-voicebot
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: OPENAI_API_KEY
        sync: false

